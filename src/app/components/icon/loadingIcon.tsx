import React from 'react'
import { Icon } from '.'

const CLASS = 'ui-small-loading-icon'

export const LoadingIcon = () => (
  <Icon icon={`spinner icon-spin icon-lg ${CLASS}`} />
)

export default LoadingIcon
