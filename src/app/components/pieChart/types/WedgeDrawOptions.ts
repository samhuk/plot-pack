import { Color } from '../../../common/types/color'

export type WedgeDrawOptions = {
  text?: string
  textColor?: Color
  fillColorHex?: string
  lineWidth?: number
}

export default WedgeDrawOptions
