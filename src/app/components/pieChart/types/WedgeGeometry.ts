import TextBoxRect from './TextBoxRect'
import { CircularSector } from '../../../common/types/geometry'

export type WedgeGeometry = {
  circularSector: CircularSector
  textBoxRect: TextBoxRect
}

export default WedgeGeometry
